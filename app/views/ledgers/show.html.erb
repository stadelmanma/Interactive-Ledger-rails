<span>
    <strong>Name:</strong>
    <%= @ledger.name%>
</span>

<span>
    <strong>Data Source:</strong>
    <%= @ledger.data_source%>
</span>

<table>
  <tr>
    <% @column_names.each do |col| %>
      <th>
        <%= col %>
      </th>
    <% end %>
  </tr>
  
  <%= render partial: 'transactions/transaction', collection: @data %>

</table>

<br>
<br>
<%= link_to 'Back', ledgers_path %>