<span>
    <strong>Name:</strong>
    <%= @ledger.name%>
</span>

<span>
    <strong>Data Source:</strong>
    <%= @ledger.data_source%>
</span>

<div>
  <div> #convert back to a table later
    <h3><%= @header %></h3>
    <% @data.each do |trans| %>
      <span>
        <%= trans %>
      </span>
      <hr>
      <Br>
    <% end %>
  </div>
</div>

<br>
<br>
<%= link_to 'Back', ledgers_path %>